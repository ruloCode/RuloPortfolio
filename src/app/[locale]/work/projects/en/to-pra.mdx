---
title: "To-Pra: The Productivity App I Built Because Nothing Else Worked"
publishedAt: "2025-02-08"
summary: "I tried every productivity app. None fit how I actually work. So I built To-Pra—a task manager with focus modes and gamification that now helps 500+ people get things done."
images:
  - "/images/projects/project-01/to-pra.jpg"
  - "/images/projects/project-01/to-pra-02.jpg"
team:
  - name: "Rulocode"
    role: "Lead Developer"
    avatar: "/images/avatar.jpg"
    linkedIn: "https://www.linkedin.com/in/rulocode/"
  - name: "Cielo"
    role: "Software Engineer"
    avatar: "/images/avatar-2.jpg"
    linkedIn: "https://www.linkedin.com/in/heavenndew/"
  - name: "Marlon Ramirez"
    role: "Software Engineer"
    avatar: "/images/avatar-3.jpg"
    linkedIn: "https://www.linkedin.com/in/marlonramirezlizcano/"
link: "https://to-pra.vercel.app/"

---

## The Problem

I'm a productivity app hoarder. Todoist, Notion, TickTick, Things 3—I've tried them all. But none of them solved my actual problem: **I'd make beautiful task lists, then ignore them.**

The issue wasn't the apps. It was me. I needed:
- Something that made completing tasks **feel rewarding**
- A built-in focus mode so I'd actually *do* the work
- Real data showing where my time went

Nothing combined all three. So I built it.

## The Solution

To-Pra is a productivity app that treats getting things done like a game—because our brains respond to rewards, not guilt.

### Core Features

**🎮 Gamified Task Completion**

Every completed task earns points. Streaks multiply rewards. You unlock achievements (Novice → Expert → Master). It sounds silly until you realize you've completed 47 tasks this week just to keep your streak alive.

**⏱️ Built-in Focus Mode**

No more switching between a task app and a timer app. To-Pra has Pomodoro built in—25, 50, or 90-minute sessions. When the timer's running, you're accountable.

**📊 Honest Analytics**

Most apps show you tasks completed. To-Pra shows you *time invested*. The difference matters. Completing 20 tiny tasks feels productive until you realize you spent 4 hours on busywork.

## Technical Decisions

### Why Next.js + Supabase?

I needed real-time sync across devices and fast iteration. Supabase gave me auth, database, and real-time subscriptions in one package. Next.js App Router let me ship features quickly while keeping the bundle small.

### The Gamification Engine

The reward system needed to feel fair but challenging:

```typescript
// Streak multiplier calculation
const getStreakMultiplier = (streak: number): number => {
  if (streak >= 30) return 3.0;  // Monthly warrior
  if (streak >= 7) return 2.0;   // Weekly consistent
  if (streak >= 3) return 1.5;   // Getting momentum
  return 1.0;                     // Starting fresh
};

// Points scale with task priority
const calculatePoints = (task: Task, streak: number): number => {
  const basePoints = task.priority * 10;
  const multiplier = getStreakMultiplier(streak);
  const focusBonus = task.completedInFocusMode ? 1.5 : 1;
  
  return Math.round(basePoints * multiplier * focusBonus);
};
```

Tasks completed during focus sessions earn 50% bonus points. This nudges users toward deep work without forcing it.

### Real-Time Sync Challenge

The hardest part was handling offline-first with eventual consistency. Users expect tasks to save immediately, even on spotty connections.

Solution: Optimistic updates with a sync queue. Changes apply instantly to the UI, then reconcile with Supabase when connection returns. Conflicts resolve by "last write wins" with timestamp comparison.

## Results

| Metric | Result |
|--------|--------|
| **Active users** | 500+ monthly |
| **Tasks completed** | 12,000+ tracked |
| **Avg. focus sessions/user** | 4.2 per day |
| **User retention (30-day)** | 42% |
| **User satisfaction** | 4.7★ average |

But the number I care about most: **I actually use it.** Every day. That's the real validation.

## What I Learned

### 1. Build for yourself first
The best products come from solving your own problems. You understand the pain deeply, and you're the first user to notice when something's off.

### 2. Gamification is a double-edged sword
Points and streaks work—until they become stressful. We added "streak freeze" after users reported anxiety about breaking chains. Features should motivate, not guilt.

### 3. Simple beats complete
Early versions had way more features (habit tracking, journaling, team boards). We cut 60% of them. The focused product retained users better than the bloated one.

## What's Next

- **Competitive weekly rankings** (opt-in) for those who thrive on competition
- **Team workspaces** for small groups
- **AI task suggestions** based on productivity patterns

---

**Built with:** Next.js 14, TypeScript, Tailwind CSS, Supabase, Vercel

> *Want to try it? [to-pra.vercel.app](https://to-pra.vercel.app/) — it's free.*
>
> *Need something similar built for your team? [Let's talk →](https://calendly.com/rulocode/30min)*
